Löscht Connection die nicht gelöscht werden sollen.
Die Connection müssen der Objekten übergeben werden
Beim Löschen der Connection müss die Connection auch aus den Objekten gelöscht werden
Ein und Ausgang müssen definiert werden
State der Ein/ Ausgänge müssen übergeben werden